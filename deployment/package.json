{
  "name": "game-nft-deployment",
  "version": "1.0.0",
  "type": "commonjs",
  "scripts": {
    "deploy": "hardhat run scripts/deploy.js --network base",
    "deploy:testnet": "hardhat run scripts/deploy.js --network baseSepolia",
    "deploy:base": "hardhat run scripts/deploy-to-base.js --network base",
    "mint:base": "hardhat run scripts/mint-to-base.js --network base",
    "test:local": "hardhat run scripts/test-full-ipfs-nft.js --network hardhat",
    "verify": "hardhat run scripts/verify.js --network base",
    "compile": "hardhat compile",
    "clean": "hardhat clean"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "dotenv": "^16.0.0",
    "hardhat": "^2.19.0"
  },
  "dependencies": {
    "form-data": "^4.0.4",
    "node-fetch": "^3.3.2"
  }
}
